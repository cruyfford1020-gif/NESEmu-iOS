import SwiftUI

enum Theme {
    static let bg = Color(red: 0.07, green: 0.07, blue: 0.09)
    static let panel = Color(red: 0.12, green: 0.12, blue: 0.15)
    static let accentRed = Color(red: 0.85, green: 0.15, blue: 0.2)
    static let accentGray = Color(red: 0.55, green: 0.55, blue: 0.6)
    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.55)
    static let screenBezel = Color(red: 0.03, green: 0.03, blue: 0.04)
}
